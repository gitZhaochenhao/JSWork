1.单位：
	px 是一个固定尺寸
	em 参照的是元素自身字符大小
	rem 参照的是html字符大小,默认也是16px
	% 尺寸大小由父元素尺寸来相对比例设置
2.不同属性用%的参照物尺寸
	a.像我们设置一个元素宽高，定位，内外边距.....的参照物都是父元素的尺寸并不是自己的尺寸
	b.transform:translate(),圆角设置，线性/径向背景渐变，background-position等为数不多的属性的参照物是自身尺寸
3.em
	em参照的是元素自身字符大小，系统默认的字符大小是16px,最小为12px  1em=自身字体大小
